APP_NAME=Laravel
# APP_ENV=local
#APP_ENV=production
APP_KEY=base64:7DqN48OrrCaJfCXTh6Dwc4Yf8c7QDArx8L0nqn4BsVg=
# APP_DEBUG=true
APP_DEBUG=true
APP_TIMEZONE=Europe/London
#APP_URL=http://<floating-ip>
APP_URL=http://localhost

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_UK

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=mariadb
#DB_HOST=10.72.99.153
DB_PORT=3306
DB_DATABASE=diffearth
DB_USERNAME=root
DB_PASSWORD=comsc

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# Gmail SMTP Configuration (Optional if using Google API directly)
MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=465
MAIL_USERNAME=clientprojecttestemail@gmail.com
MAIL_PASSWORD=oulrvhrgaoqbqskm
MAIL_ENCRYPTION=ssl
MAIL_FROM_ADDRESS=clientprojecttestemail@gmail.com
MAIL_FROM_NAME='Chil Monitoring Service'

# Pusher config
BROADCAST_DRIVER=pusher
PUSHER_APP_ID="1966684"
PUSHER_KEY="799c51317ac01f63634c"
PUSHER_SECRET="c659edea3987152f96a4"
PUSHER_CLUSTER="eu"

VITE_PUSHER_KEY="${PUSHER_KEY}"
VITE_PUSHER_CLUSTER="${PUSHER_CLUSTER}"

VITE_APP_NAME="${APP_NAME}"
VITE_HOST=<floating-ip>

